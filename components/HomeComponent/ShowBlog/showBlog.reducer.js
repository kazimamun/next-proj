import blogInit from "./showBlog.initialState";

const blogReducer = (state=blogInit, action) =>{
    switch(action.type){
        default :
            return state
    }
};

export default blogReducer;